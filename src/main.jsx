import React from "react";
import ReactDOM from "react-dom/client";
import SignalApp from "./signal-app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SignalApp />
  </React.StrictMode>
);
